# 音乐与内容 MCP 部署记录（2026-09-08）

## 部署范围

用户授权复用旧服务器 47.108.114.17 的网易云和 MiniMax 凭据，在 39.107.143.71 部署，并为连接此自建小智的全部板子配置工具。旧服务器连接器保持运行。

来源为 D:/xiaozhi_music_mcp/server 当前工作树，含未提交的音乐搜索修复及 content_tools.py。部署源码副本在 /opt/xiaozhi-music-aipet/server；独立镜像 xiaozhi-music-aipet:20260908 基于线上 b13 镜像构建，未覆盖语音镜像。

## 服务与私有配置

- /opt/xiaozhi-music-aipet/compose.json：music-gateway 和 music-content-mcp 两个容器，restart=unless-stopped。
- 网关绑定宿主机 127.0.0.1:3060；MCP 仅通过 xiaozhi-server_default 内网的 http://music-content-mcp:3061/mcp 访问，无公网映射。
- 音乐和 MiniMax 凭据分别保存在部署目录的 music.env、content.env，0600，父目录 0700；禁止入 Git 或输出内容。
- 全局接入：/opt/xiaozhi-server/data/.mcp_server_settings.json，新增 aipet_music_content；保留原有其他 MCP 配置，transport=streamable-http。每次新会话加载。
- 签名音频与歌词使用 http://39.107.143.71:8081/audio_ogg/ 和 /lyric/ 路径，由 App Nginx 转发至回环网关。签名路径关闭访问日志；搜索接口未向公网代理。
- App 配置真源 ai-pet-app/deploy/nginx.conf；线上 /opt/ai-pet/ai-pet-app/nginx.conf 已同步并通过 nginx -t 和 reload。

四工具：search_netease_music、zodiac_fortune、metaphysics_analysis、daily_chat。内容工具不读业务历史，不写记忆，不主动播报；上下文由调用方显式传入。

## 板子覆盖及验收边界

智控台登记 4 块实体板子：P4（8c:fd:49:0c:a8:78、8c:fd:49:0c:b2:44）、S3 LCD EV（90:e5:b1:a8:ed:80）、BOX-3（b4:3a:45:f3:9b:70），另有 sim-test。均归属启用 Intent_function_call 的智能体，因此统一加载全局工具；以后接入此服务且启用工具调用的板子沿用该配置。

本地 AI Pet 固件中未发现 self.music.play_url，不能据此宣称这些板子已能播放音乐。音乐搜索与内容工具的服务端接入已完成；逐板需核对实际固件工具清单，缺少播放器时需另补固件并烧录。未对任何板子烧录，未修改其 OTA/网络凭据。

## 已完成验证

- 目标服务器 24 项单元/HTTP 契约测试通过。
- 网关 health 返回 ok、account_configured=true。
- 真实 MCP tools/list 四工具齐全；线上小智原生 ServerMCPClient 初始化成功并列出四工具。
- 邓紫棋《超能力》真实搜索命中 1833633769；公网签名音频读取 8192 字节，OggS 头验证通过。
- 三类内容工具均完成真实 MiniMax 请求，success=true，返回正文长度分别为 203、291、210。
- 以上属于服务端/公网流验证；设备语音触发及扬声器播放仍待真机验收。

## 维护和回滚

运行命令：cd /opt/xiaozhi-music-aipet 后执行 docker compose -f compose.json ps、up -d、stop。

本次脚本/构建日志位于 /opt/xiaozhi-music-stage-XHruglZa；integration-backup 为 0700，保存原 nginx.conf 及原全局 MCP 配置（若原配置不存在则有 settings-was-absent 标记）。回滚时先核对是否有后续并行配置，移除本次 aipet_music_content 项和音频代理段，nginx -t 后 reload，再停止本次两个容器。不要覆盖后续其他 MCP 配置，不要停止旧服务器服务。
