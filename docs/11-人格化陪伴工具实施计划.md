# 人格化陪伴工具实施计划（2026-09-08）
用户已要求完成全部六项能力并自动验证、构建部署及提交推送。
## 交付
1. 12星座、16MBTI独立版本化策略档案，覆盖192组合及忙碌/疲惫/冲突等情境；优先级：明确且已批准的偏好 > 当前情境 > 主人沟通偏好种子 > 宠物风格。
2. 六工具：conversation_plan、context_search、companion_reply、interaction_feedback、shared_activity、followup_plan。共用策略/上下文；旧daily_chat等继续可用并接入后端。事实生成必须MiniMax检索，不成功不回退。
3. backend提供当前设备当前主人的上下文、候选反馈及约定持久化；反馈复用Memory审核，约定新增独立表和迁移。不得读取重绑前主人的数据。
4. 小智按连接注入签名鉴权所需头和设备/会话身份，身份不作为模型可填写的工具参数。缓存检索证据按设备和会话隔离，只在同一任务短时复用，避免重复搜索。
5. 到期约定在08:00至21:00东八区的合适会话中提示；提供列表、完成和取消，不将提示视为已完成，不承诺离线定点唤醒。这是会话内跟进，不解除E11独立控制通道的冻结。
6. 本地规则/隔离测试、后端ruff+mypy+pytest、临时PostgreSQL迁移与真实API测试、候选镜像MCP调用；再部署39.107.143.71，核验数据库、实际工具和服务，记录真实硬件未验证边界。
## 仓库
## 完成状态（2026-09-08）

六工具和28份策略已部署；43项内容侧测试、backend 139项测试及ruff/mypy通过。临时PostgreSQL验证迁移、审批、换绑隔离、脱敏及MCP约定写入；真实MiniMax检索/复用和四块实体板原生MCP只读接入、重连验证通过。未烧录固件，扬声器实播与本人体验仍待验收。

用户新增朋友的赛博分身小智云接入点：独立普通用户/虚拟设备cloud-agent-2323485，不复用宠物数据；额外提供memory_search/add/forget及emotion_support，共14工具。20:14北京时间云端initialize/tools-list通过，桥接确认注册14工具。用户已明确授权相关个人数据向MiniMax出站；令牌和账号凭据仅在服务器私有配置。

生产：backend 9be6235三应用同镜像；小智b14-companion；内部MCP20260908-companion；云桥20260908-cloud。备份与验证日志位于/opt/aipet-companion-yCjfbMYD。两份用户说明同步写入D:/ESP32-s3-Touch-LCD-1.85C-BOX及D:/xiaozhi_music_mcp根目录。

## 仓库分工
backend负责数据真源；D:/xiaozhi_music_mcp负责策略和工具；xiaozhi-server负责身份传递和部署；work_dashboard仅同步事实。音乐源仓无远端，先本地提交，不猜测远端。
