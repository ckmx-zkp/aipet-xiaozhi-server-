# 06 — 开发进度与后续计划（xiaozhi-server）

> 最后核实：2026-08-16。代码仓、线上镜像和看板的状态可能不同；线上运行版本以
> `D:/Home_Work/work_dashboard/AI-Pet协作看板.md` 为准。

## 当前线上基线

- 线上语音镜像：`xiaozhi-aipet-server:v0.9.6-b9`（2026-08-16；b8 当天由 b6 直跳、
  b7 废弃，b9 = b8 + 旁路统一日志）；真机 MAC `8c:fd:49:0c:a8:78` 已完成 OTA、激活和基础语音对话。
- 语音链路：MiniMax-M2.5 主 LLM（GLM/千帆/Kimi 备用）、豆包流式 ASR、火山流式 TTS（24 kHz）。
- Prompt 最终组成：服务固定行为库 `pet_default` → backend `persona_pack`
  （星座/MBTI）→ Context Provider 动态上下文摘要（b8 已上线，待真机验收）。智控台的旧角色介绍不会作为最终
  人设覆盖此顺序。
- 业务旁路：设备首见、用户/助手消息、会话结束、眼睛状态均已实现；尚缺真机落库
  证据。

状态含义：`已部署` = 已在当前线上镜像；`待验收` = 代码存在但尚无完整真机证据；
`阻塞` = 依赖其他仓或产品决策。

## Epic A — 自建云与设备接入

- [x] A1 钉住上游 v0.9.6 源码快照并维护自建镜像。
- [x] A2 Compose 全模块上线（WS/OTA/智控台）；域名、TLS/WSS 仍未完成。
- [x] A3 OTA 公网地址、`server.websocket` 和激活配置已校正并文档化。
- [x] A4 真机激活、首轮 ASR→LLM→TTS 和眼睛动作通路已验证。
- [ ] A5 真机回归脚本：重连、断网恢复、首轮语音、采样率和延迟记录。

## Epic B — 人设与基础行为运行时

- [x] B1 `persona_pack`：连接首拉 + 默认 300 秒刷新，remote → 缓存 → onboarding
  降级。
- [x] B2 pack 变化时替换 Prompt，并映射 `default_emotion` 到眼睛 MCP。
- [x] B3 固定基础行为库：已加载 `xiaozhi_ai_pet_base_behavior_prompt_library.md`
  的 `pet_default`，限制冗长回复、动作后延聊和打断后续讲。
- [x] B4 App 修改后的天蝎 + ENFP `persona_pack` 已由线上 backend 返回验证。
- [ ] B5 真机验收：修改人设后重连或等待一个刷新周期，确认语气/默认表情改变且不
  重复触发未变化 pack。
- [x] B6 Context Provider 最终 Prompt 合并：代码+部署完成（b8，2026-08-16），
  待真机验收；`persona_pack` 不再覆盖 `{{ dynamic_context }}`（见 Epic D）。

## Epic C — 旁路、会话与外设

- [x] C1 连接 `devices/seen`：MAC 规范化并异步首见登记。
- [x] C2 `chat/events`：原生字符串 UUID、异步重试、用户/助手文本旁路。
- [x] C3 `chat/sessions/{session_id}/end`：连接结束调用一次，可重试。
- [x] C4 `peripheral/events`：五个眼睛 MCP 成功后更新全量状态快照。
- [ ] C5 真机 E2E 证据：一次会话取得 seen、user、assistant、session end、眼睛状态
  五类 backend 落库记录；失败日志不得含 token 或对话原文。
- [x] C6 可观测性补强（b9，2026-08-16）：新增 `core/utils/integration_log.py` 统一旁路日志
  （tag=BIZ），persona_pack/chat events/session end/peripheral/C5 均记录 `device_uid`、
  `session_id`、耗时、结果与降级原因；4xx 丢弃、5xx 有界重试、重试中间过程仅 debug；
  不含 token/对话原文/完整 Prompt；供后续 Memory MCP 复用。真机日志证据随 C5 一起取。

## Epic D — Backend Context Provider（跨仓依赖）

- [x] D1 核对上游协议：小智会在 Prompt 构建时对每个配置源发 GET，自动带
  `device-id`，只接受 `{"code":0,"data":...}`。
- [x] D2 backend C5 已上线：`GET /api/internal/context/device`，按 `device-id`
  返回当前设备相关的已发布 KB 短摘要；不返回完整 KB、原始对话或敏感数据。
- [x] D3 本仓接入完成（b8，2026-08-16）：本地容器私有配置内部 URL +
  `X-Internal-Token`，不在智控台、仓库或看板保存密钥。
- [x] D4 固定行为、人设 pack 和 `dynamic_context` 已合并为一个最终 system prompt
  （b8）：“唤醒只拉一次、慢/空/5xx 不阻塞首轮语音”容器级已验，真机验收待做。

## Epic E — 对话稳定性与宠物交互

- [x] E1 新语音或中断时使旧 LLM/MCP 任务和流失效，避免旧工具调用迟到执行。
- [x] E2 S1–S4 已部署：关闭唤醒问候、无语音 30 秒断连、下行 24 kHz；休息类请求
  成功闭眼后播放短确认并主动断开。
- [x] E3 S5 服务端路由复核：`self_eye_look`、`blink`、`close`、`open`、
  `set_emotion` 保持设备协议原样。
- [ ] E4 真机回归：休息后不再出现延迟告别语；24 kHz 后无重采样告警；五个眼睛工具
  全量成功。
- [ ] E5 输出体验验收：默认回答 1–2 句、工具成功只短确认、打断后不续讲；必要时再
  调整 `pet_default`，不把约束写回 backend 用户人设。

## Epic F — 长期记忆与稳态

- [ ] F1 **阻塞 backend**：Memory MCP 实库和双方确认的 HTTP/streamable 契约。
- [ ] F2 挂载 Memory MCP，设置 0.8–1.5 秒超时与“无记忆会话”降级。
- [ ] F3 回归：记忆检索不拖慢首字或动作工具。
- [ ] F4 Docker CPU/内存限额与 Agent 隔离。
- [ ] F5 上游小版本升级演练，回归 A5、B5、C5、E4。

## 近期执行顺序

1. 真机完成 E4 + C5，优先验证休息断开、24 kHz 和消息/外设落库。
2. ~~backend 完成 C5 Context Provider 后，完成本仓 D3–D4 联调。~~ 已完成（b8，
   2026-08-16）；仅剩真机验收（think 不播报、首轮体现 C5 上下文、backend 宕机降级）。
3. backend 定稿并实现 Memory MCP 后，推进 F1–F3。
4. 连续体验稳定后再处理 TLS/WSS、资源限额和上游升级演练。

## 本仓 V0.2 完成定义

设备可经自建 OTA 接入并稳定对话；固定基础行为与 backend 人设可刷新生效；设备
首见、消息、会话结束与外设状态均有真机 E2E 证据；休息/打断/眼睛工具和 24 kHz
通过回归。Context Provider 与 Memory MCP 属跨仓后续依赖，未实现时不阻断前述基础链路。
